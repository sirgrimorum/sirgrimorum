name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Setup runtime
        run: echo "replace with runtime setup"
      - name: Install dependencies
        run: make install
      - name: Run CI checks
        run: make ci
