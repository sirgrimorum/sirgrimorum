#!/usr/bin/env bash
set -euo pipefail

echo "This script is intentionally disabled."
echo "Reason: adapt/upgrade/finalize are agent-guided workflows, not deterministic hook actions."
echo "Use the skill directly: /agent-governance with mode adapt|upgrade|finalize."
exit 1
